/**
 * Transport module for logger package.
 * Provides the ConsoleTransport class for logging output to the console.
 * ConsoleTransport implements a transport interface for flexible log routing.
 */
export { ConsoleTransport } from "./ConsoleTransport";
